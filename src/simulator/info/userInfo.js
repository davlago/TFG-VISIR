/**
 * Clase User Info, tiene toda la información
 */

import EntityInfo from "../../engine/entities/entityInfo";

export default class UserInfo extends EntityInfo{

    constructor(data, color) {
        super(data, color);
    }
    
}